# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

orbs:
  test-orb: klsember/test-orb@dev:21c8886

jobs:
  test-clock:
    docker:
      - image: aldanial/cloc:1.90
    steps:
      - checkout
      - run: 
          command: cloc .
      
workflows:
  test:
    jobs:
      - test-clock
      - test-orb/greet
      
